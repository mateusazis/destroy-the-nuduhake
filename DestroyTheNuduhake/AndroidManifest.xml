<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uff.br.pse.destroythenuduhake"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" android:targetSdkVersion="17" />
    
    <!-- To communication with the DTN service we need the permission to do so -->
    <uses-permission android:name="de.tubs.ibr.dtn.permission.DTN_COMMUNICATION"/>
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name="uff.br.pse.destroythenuduhake.MainActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter >
        		<action android:name="de.tubs.ibr.dtn.intent.DTNAPP" />
       			<category android:name="android.intent.category.DEFAULT" />
    </intent-filter>
        </activity>
        <activity android:name="uff.br.pse.destroythenuduhake.DisplayAssetsActivity"></activity>
        
        
        <service android:name="uff.br.pse.destroythenuduhake.DTNService" />
        
	    <receiver
	        android:enabled="true"
	        android:name="uff.br.pse.destroythenuduhake.DTNReceiver"
	        android:exported="true"
	        android:permission="de.tubs.ibr.dtn.permission.DTN_SERVER">
	        <!-- Add intent filter for notification of incoming bundles -->
	        <intent-filter>
	            <action android:name="de.tubs.ibr.dtn.intent.RECEIVE" />
	            <category android:name="uff.br.pse.destroythenuduhake" />
	        </intent-filter>

	    </receiver>
    </application>

</manifest>